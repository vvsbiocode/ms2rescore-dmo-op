version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    install:
      - pip install --upgrade pip
      - pip install --group 'docs' .

sphinx:
  configuration: docs/source/conf.py
  builder: dirhtml
